name: self-hosted
on:
  workflow_dispatch:
jobs:
  info:
    runs-on: self-hosted
    steps:
      - name: Checking following information about self-hosted runner
        run: |
          echo "Hostname: $HOSTNAME"
          echo "Working directory: $PWD"
          echo "Hi from github" > hi.txt