name: environment variable test
on:
  workflow_dispatch:
env:
  APP_NAME: myapp
jobs:
  test:
    env:
      ENVIRONMENT: staging
    runs-on: ubuntu-latest
    steps:
      - name: accessing from same job
        run: |
          echo "APP_NAME :" $APP_NAME
          echo "ENVIRONMENT :" $ENVIRONMENT
          echo "VERSION :" $VERSION
        env:
          VERSION: 1.0.0
  access:
    runs-on: ubuntu-latest
    steps:
      - name: accessing from different job
        run: |
          echo "APP_NAME :" $APP_NAME
          echo "ENVIRONMENT :" $ENVIRONMENT
          echo "VERSION :" $VERSION
